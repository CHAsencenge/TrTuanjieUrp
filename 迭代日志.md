# 迭代日志

> 本文件记录项目每次迭代的详细变更内容，包括范围、修改文件、设计决策、测试结果等。
> 按时间倒序排列，最新的迭代在最上方。

---

## [2026-03-30] 移植 UE5 高性能 Shader 基础库到 TrRender

### 范围
- 将 UE5 独有的高级 shader 工具函数移植到 `Packages/com.tr.render/ShaderLibrary/UE/`
- 覆盖 PCG 随机数、程序化噪声、高级漫反射模型、EnvBRDF 近似、蒙特卡洛重要性采样、球面高斯、色温白平衡等
- 所有代码添加中文关键注释

### 变更

**新增目录 `Packages/com.tr.render/ShaderLibrary/UE/` (11 个 HLSL 文件, ~2036 行)**

Phase 1 基础层:
- `FastMath.hlsl` (195行) — 快速 exp/log、IEEE 754 位操作近似算术、多项式三角函数
- `Hash.hlsl` (49行) — MurmurHash3 增量混合、PCG 单值哈希
- `RandomPCG.hlsl` (105行) — 3D/4D PCG 随机数、16/32bit 浮点转换

Phase 2 功能层:
- `BRDFExtra.hlsl` (180行) — BxDFContext 预计算、Burley/OrenNayar/EON 漫反射、EnvBRDFApprox 纯 ALU 环境 BRDF
- `MonteCarlo.hlsl` (327行) — Duff/Frisvad 正交基、球面/半球/锥体采样、VNDF 可见法线采样、MIS 权重
- `Noise.hlsl` (343行) — Perlin 梯度噪声、Simplex + Jacobian 矩阵、多质量 Voronoi
- `ColorSpaceExtra.hlsl` (228行) — CIE LAB/LCH、YCoCg 变体、平滑 HSV、LMS 色彩空间
- `TonemapExtra.hlsl` (246行) — FilmToneMap S 曲线、Hable/Reinhard 色调映射、Bradford 色度适应白平衡
- `SphericalGaussian.hlsl` (221行) — SG/ASG 求值/积分/归一化/乘法/内积/卷积、AO→SG 近似
- `TextureSamplingExtra.hlsl` (120行) — Bicubic Catmull-Rom 5-tap 优化双三次采样

Phase 3 汇总:
- `UEShaderLibrary.hlsl` (22行) — 一次 include 引入全部 UE 工具

### 设计决策
- **仅移植 ALU 版本**: 跳过所有依赖 UE5 纹理资源 (View.PerlinNoiseGradientTexture) 的函数
- **UE5→Unity 适配**: `Pow2()`→`Sq()`, `Square()`→`Sq()`, `length2(x)`→`dot(x,x)`, `UNROLL_N(n)`→`[unroll(n)]`, `select()`→三元/lerp, `rcpFast()`→保留在 FastMath 中
- **补充缺失函数**: URP Core 缺少 `Pow5()`, 在 BRDFExtra.hlsl 中自定义补充
- **函数重命名避免冲突**: SG 函数加 `SG_` 前缀 (Evaluate→SG_Evaluate) 避免与其他库冲突, ASG 函数加 `ASG_` 前缀
- **FilmToneMap 参数化**: 将 UE5 的 uniform 变量 (FilmSlope 等) 改为函数参数，解除 CBUFFER 依赖
- **白平衡内联实现**: Bradford 色度适应矩阵直接内联，避免依赖 UE5 的 ChromaticAdaptation 函数
- **ColorSpace 函数加 `UE_` 前缀**: 避免与 URP Core 已有的同名函数 (如 HSV_2_LinearRGB) 冲突

### 依赖图
```
FastMath ──┐
Hash ──────┤── Noise
RandomPCG ─┘
               BRDFExtra ──────┐
               MonteCarlo ─────┤
               ColorSpaceExtra ┤── UEShaderLibrary
               TonemapExtra ───┤
               SphericalGaussian┤
               TextureSamplingExtra┘
```

### 测试
- 静态检查: 无 `select()`, `length2()`, `Square()`, `Pow2()`, `UNROLL_N()`, `MaterialFloat` 等 UE5 残留
- 编译验证: .hlsl include 文件需在被 .shader 引用时验证，现有 Editor Log 无新增 Shader error

### 备注
- 用户可通过 `#include "Packages/com.tr.render/ShaderLibrary/UE/UEShaderLibrary.hlsl"` 一次性引入全部功能
- 也可按需单独 include 特定文件以减少编译时间
- UE5 源码参考路径: `C:\UE\TrUETestbed\Engine\Shaders\Private\`

---

## [2026-03-30] 将 URP/Core ShaderLibrary 迁移到 TrRender Package

### 范围
- 将 TrRender 包引用的所有 URP 和 Core RP shader 文件（含传递依赖）复制到包内
- 更新所有 include 路径，使 shader 层完全自包含于 `Packages/com.tr.render/`
- C# 侧保持 asmdef 引用 URP Runtime 程序集（ScriptableRendererFeature 扩展点无法避免）

### 变更

**新增目录结构 (Packages/com.tr.render/)**
- `CoreShaderLibrary/` - Core RP ShaderLibrary 副本 (20 根目录文件)
- `CoreShaderLibrary/API/` - 平台图形 API 适配 (9 文件: D3D11, Metal, Vulkan, GLCore, GLES2/3, Switch, Validate, FoveatedRendering_Metal)
- `CoreShaderLibrary/Sampling/` - 采样工具 (3 文件: Fibonacci, Hammersley, Sampling)
- `CoreShaderLibrary/Shadow/` - 阴影采样 (1 文件: ShadowSamplingTent)
- `CoreRuntime/` - Core Runtime shader 生成文件 (1 文件: Hammersley.cs.hlsl)
- `ShaderLibrary/` - URP ShaderLibrary 副本 (23 根目录文件)
- `ShaderLibrary/Debug/` - 调试工具 (3 文件)
- `ShaderLibrary/LightCookie/` - 光照 Cookie (3 文件)
- `ShaderPasses/` - URP Shader Pass 副本 (2 文件: ShadowCasterPass, DepthOnlyPass)
- `ShaderConfig/` - URP Config (1 文件: ShaderConfig.cs.hlsl)

**合计: 66 个 HLSL 文件复制并路径替换**

**修改 (自定义 Shader include 路径更新)**
- `Shaders/CustomDeferredLitInput.hlsl` - Core.hlsl, SurfaceInput.hlsl → `com.tr.render/ShaderLibrary/`
- `Shaders/CustomGBuffer.hlsl` - Packing.hlsl → `com.tr.render/CoreShaderLibrary/`
- `Shaders/CustomBRDF.hlsl` - BRDF.hlsl → `com.tr.render/ShaderLibrary/`
- `Shaders/CustomDeferredLight.hlsl` - Core.hlsl, Lighting.hlsl → `com.tr.render/ShaderLibrary/`
- `Shaders/CustomGBufferPass.shader` - ShadowCasterPass.hlsl, DepthOnlyPass.hlsl → `com.tr.render/ShaderPasses/`
- `Shaders/CustomDeferredLighting.shader` - DeclareDepthTexture.hlsl → `com.tr.render/ShaderLibrary/`

### 设计决策
- **镜像目录结构**: CoreShaderLibrary/ 对应 Core RP ShaderLibrary/，ShaderLibrary/ 对应 URP ShaderLibrary/，保持文件组织清晰
- **保留平台特定引用**: PS4/PS5/Xbox/GameCore 的 `#include` 保留原始路径，因为这些在 `#if` 平台宏后面且对应包不存在于 PC 开发环境
- **保留 VFX Graph 引用**: `Input.hlsl` 中的 VFX Graph include 在 `#ifdef HAVE_VFX_MODIFICATION` 后面，不影响编译
- **C# 侧不变**: asmdef 继续引用 URP Runtime 程序集，因为 ScriptableRenderPass/ScriptableRendererFeature 是 URP 的公共扩展点

### 路径映射表
| 原始路径前缀 | 替换为 |
|-------------|--------|
| `com.unity.render-pipelines.core/ShaderLibrary/` | `com.tr.render/CoreShaderLibrary/` |
| `com.unity.render-pipelines.core/Runtime/.../Hammersley.cs.hlsl` | `com.tr.render/CoreRuntime/Hammersley.cs.hlsl` |
| `com.unity.render-pipelines.universal/ShaderLibrary/` | `com.tr.render/ShaderLibrary/` |
| `com.unity.render-pipelines.universal/Shaders/` | `com.tr.render/ShaderPasses/` |
| `com.unity.render-pipelines.universal-config/Runtime/ShaderConfig.cs.hlsl` | `com.tr.render/ShaderConfig/ShaderConfig.cs.hlsl` |

### 测试
- C# 编译: 通过 (`LogAssemblyErrors (0ms)`)
- Shader 编译: Editor Log 无 `Shader error`
- 自包含验证: 包内 118 个内部引用，仅 6 个外部引用（均在平台/VFX `#if` 守卫后面）

### 备注
- 编辑器需重新获得焦点以触发 shader 重新导入和编译验证
- 未来修改 URP shader 逻辑时直接在包内修改，无需同步外部包
- 包版本可考虑升至 0.3.0 以反映此次重大结构变更
---

## [2026-03-30] 自定义延迟渲染管线整合为 Local Package

### 范围
- 将 Assets/ 下的自定义延迟渲染代码迁移至 Unity Local Package (`Packages/com.game.rendering.deferred/`)
- 遵循 URP 包目录结构约定 (Runtime/, Shaders/, Tests/)
- 更新 shader include 路径为包绝对路径格式
- 清理 Assets/ 中的旧文件

### 变更

**新建 Package 结构 (Packages/com.game.rendering.deferred/)**
- `package.json` - 包描述: name=com.game.rendering.deferred, version=0.2.0, 依赖 URP 14.1.0 + Core 14.1.0
- `Runtime/Game.Rendering.Runtime.asmdef` - 运行时程序集定义
- `Runtime/Core/` - ShadingModelId.cs, CustomDeferredConfig.cs, CustomDeferredRendererFeature.cs
- `Runtime/Passes/` - CustomGBufferPass.cs, CustomDeferredLightingPass.cs
- `Runtime/Utils/` - GBufferUtils.cs
- `Shaders/` - 7 个着色器文件 (5 .hlsl + 2 .shader)，include 路径更新为 `Packages/com.game.rendering.deferred/Shaders/` 格式
- `Tests/Editor/` - 3 个测试文件 + Game.Tests.Editor.asmdef
- `Tests/Runtime/` - 1 个测试文件 + Game.Tests.Runtime.asmdef

**修改**
- `Packages/manifest.json` - 添加 `testables: ["com.game.rendering.deferred"]` 以支持 Test Runner 发现包内测试

**删除 (从 Assets/ 清理)**
- `Assets/Scripts/Runtime/Rendering/` - 全部删除 (已迁移至 Package)
- `Assets/Shaders/CustomDeferred/` - 全部删除 (已迁移至 Package)
- `Assets/Tests/` - 全部删除 (已迁移至 Package)

### 设计决策
- **Embedded Package**: 选择直接放置在 `Packages/` 目录下的嵌入式包模式，Unity 自动检测 package.json，无需修改 manifest.json 中的依赖项
- **Shader include 路径**: 所有 HLSL 内部引用从相对路径 (`"CustomXxx.hlsl"`) 改为包绝对路径 (`"Packages/com.game.rendering.deferred/Shaders/CustomXxx.hlsl"`)，确保在包目录结构下正确解析
- **保持 asmdef 名称不变**: `Game.Rendering.Runtime`、`Game.Tests.Editor`、`Game.Tests.Runtime` 名称保持一致，避免引用断裂
- **testables 配置**: 在 manifest.json 添加 testables 项以确保 Test Runner 能发现并运行包内测试

### 测试
- C# 编译: 通过 (`LogAssemblyErrors (0ms)`)
- Shader 编译: 通过 (无 `Shader error`)
- 测试用例: 49 个 (与迁移前一致，未新增)

### 备注
- 包版本设为 0.2.0 (对应 Phase 2 功能完成)
- 未来可通过 Git URL 方式将此包引用到其他项目
- 编辑器中 Package Manager 窗口应能看到 "Custom Deferred Rendering" 包 (In Project 分类)
---

## [2026-03-30] Phase 2: 扩展着色模型 - Subsurface / ClearCoat / TwoSidedFoliage

### 范围
- 在自定义延迟渲染管线中实现三个新的着色模型: Subsurface、ClearCoat、TwoSidedFoliage
- 参考 UE5 ShadingModels.ush 架构，适配已有的 GBuffer CustomData 通道
- 新增对应的单元测试

### 变更

**HLSL 着色器代码 (Assets/Shaders/CustomDeferred/)**
- `CustomBRDF.hlsl` - 新增 ClearCoat 辅助函数 (ClearCoatFresnel, ClearCoatSpecular, ClearCoatEnergyPreservation)、Subsurface 辅助函数 (WrapDiffuse)、TwoSidedFoliage 辅助函数 (FoliageTransmission)
- `CustomShadingModels.hlsl` - 实现三个着色模型评估函数:
  - `EvaluateSubsurface()` - 包裹光照 + 次表面颜色混合 + 标准高光
  - `EvaluateClearCoat()` - 双层材质: F0=0.04 介电质涂层 GGX + 基底层能量守恒衰减
  - `EvaluateTwoSidedFoliage()` - 正面标准 PBR + 背面包裹漫反射透射 + 视角散射
  - 更新分发函数 `EvaluateShadingModel()` 添加 `customData` 参数，保留向后兼容重载
- `CustomDeferredLight.hlsl` - EvaluateMainLight/EvaluateAdditionalLights 传递 `gbufferData.customData` 到着色模型分发

**测试代码 (Assets/Tests/Editor/)**
- `GBufferPackingTests.cs` - 新增 7 个测试: CustomData 编码 round-trip (Subsurface 颜色+不透明度、ClearCoat 强度+粗糙度、TwoSidedFoliage 背面颜色、通道独立性、模型ID+标志组合)
- `ShadingModelTests.cs` - 新增 5 个测试: Phase 2 模型 ID 匹配 HLSL 宏定义、顺序 ID 验证、所有 Phase 2 模型与所有标志组合的编码验证

### 设计决策
- **Subsurface 简化实现**: 使用 WrapDiffuse (wrapFactor=0.5) 代替 UE5 的 Profile 纹理查找，适合实时渲染且无需预计算 LUT
- **ClearCoat 双层模型**: 顶层 F0=0.04 (IOR≈1.5 聚氨酯)，底层通过 `(1-F_coat)²` 能量守恒衰减，参考 UE5 但简化折射投影
- **TwoSidedFoliage 透射**: 包裹 NdotL + Pow4(VdotL) 视角散射近似前向散射相位函数，参考 UE5 但去除 SGGX 复杂度
- **CustomData 通道分配**: 直接使用 GBuffer2.ba (customData.xy) + GBuffer4.rg (customData.zw)，无需修改 GBuffer 布局
- **向后兼容**: 保留 7 参数 `EvaluateShadingModel` 重载（DefaultLit 不需要 customData），新增 8 参数版本

### 测试
- 新增测试用例: 12 个 (GBuffer: 7, ShadingModel: 5)
- 总测试用例: 49 个 (Editor: 43, Runtime: 6)
- C# 编译: 通过 (Editor Log: `LogAssemblyErrors (0ms)`)
- Shader 编译: 通过 (Editor Log: 无 `Shader error`)

### 备注
- CustomData 在 GBuffer 中以 R8G8B8A8_UNorm 存储，精度为 8-bit (1/255)，对颜色类数据足够
- ClearCoat 使用 Pow4 近似 Pow5 Schlick Fresnel 以提升移动端性能
- Phase 3 (Mobile 优化) 中 CustomData 通道在 3RT 布局下不可用，移动端不支持 Subsurface/ClearCoat/TwoSidedFoliage
---

## [2026-03-30] 自定义延迟渲染管线 Phase 1 实现

### 范围
- 建立自定义延迟渲染管线（Custom Deferred Rendering Pipeline），不透明物体走 Deferred，透明物体走 Forward
- 仅支持 DefaultLit 着色模型，PC 平台，Phase 1 最小可用管线
- 参考 UE5 GBuffer 架构 + URP RendererFeature 注入模式

### 变更

**C# 运行时代码 (Assets/Scripts/Runtime/Rendering/)**
- `Core/ShadingModelId.cs` - 着色模型枚举 (DefaultLit=0, Subsurface=1, ClearCoat=2, TwoSidedFoliage=3, Unlit=15) + MaterialFlags 位标志
- `Core/CustomDeferredConfig.cs` - GBuffer 配置 ScriptableObject，PC 5RT / Mobile 3RT 布局，GraphicsFormat 定义
- `Core/CustomDeferredRendererFeature.cs` - 主 RendererFeature，注入 GBuffer Pass + Deferred Lighting Pass
- `Passes/CustomGBufferPass.cs` - GBuffer 写入 Pass，使用 ShaderTagId("CustomDeferredGBuffer") 过滤，RTHandle MRT 管理
- `Passes/CustomDeferredLightingPass.cs` - 延迟光照 Pass，全屏三角形绘制，读取 GBuffer + 深度重建世界坐标
- `Utils/GBufferUtils.cs` - GBuffer RT 分配工具 + ShadingModelId/MaterialFlags C# 侧编码解码
- `Game.Rendering.Runtime.asmdef` - Assembly Definition，引用 URP Runtime + Core Runtime

**HLSL 着色器代码 (Assets/Shaders/CustomDeferred/)**
- `CustomGBuffer.hlsl` - GBuffer 编码/解码函数，八面体法线编码，PC 5RT + Mobile 3RT 布局
- `CustomDeferredLitInput.hlsl` - 材质输入结构，CBUFFER 定义，纹理采样函数
- `CustomBRDF.hlsl` - 自定义 BRDF 评估，基于 URP BRDF.hlsl 扩展，GGX 高光
- `CustomShadingModels.hlsl` - 着色模型分发系统 (Phase 1 仅 DefaultLit)
- `CustomDeferredLight.hlsl` - 延迟光照核心：世界坐标重建、主光源/附加光源评估、SH 环境光
- `CustomGBufferPass.shader` - GBuffer 写入 Shader (LightMode=CustomDeferredGBuffer) + ShadowCaster + DepthOnly
- `CustomDeferredLighting.shader` - 延迟光照全屏 Shader，Additive Blend

**测试代码 (Assets/Tests/)**
- `Editor/GBufferPackingTests.cs` - GBuffer 编码解码 round-trip 测试 (12 个测试用例)
- `Editor/ShadingModelTests.cs` - 着色模型 ID 唯一性、范围、标志位有效性测试 (8 个测试用例)
- `Editor/CustomDeferredConfigTests.cs` - GBuffer 配置格式验证、PC/Mobile 差异测试 (11 个测试用例)
- `Runtime/DeferredPipelineIntegrationTests.cs` - 管线集成测试 (6 个测试用例)
- `Editor/Game.Tests.Editor.asmdef` + `Runtime/Game.Tests.Runtime.asmdef` - 测试 Assembly Definition

**项目配置**
- `ProjectSettings.asset` - Standalone 平台添加 `ENABLE_DEBUG_LOG;ENABLE_UNIT_TESTS` 宏定义

### 设计决策
- **注入模式而非修改 URP**: 通过 ScriptableRendererFeature 注入，所有代码在 Assets/ 目录下，保持 URP 版本升级兼容性
- **自定义 LightMode 标签**: 使用 `CustomDeferredGBuffer` 标签，URP Forward 不识别此标签，自然跳过这些对象
- **GBuffer 布局参考 UE5**: PC 5RT 包含 BaseColor+Flags / Normal+Smoothness+Metallic / Specular+AO+CustomData / Emissive / CustomData 扩展
- **ShadingModelId 4-bit 编码**: 低 4 位存储着色模型 ID (0-15)，高 4 位存储材质标志，参考 UE5 DeferredShadingCommon
- **CommandBufferPool 模式**: Execute() 中使用 CommandBufferPool.Get/Release 而非 renderingData.commandBuffer (后者为 internal 访问级别)
- **CBUFFER 中 uint→float**: SRP Batcher 和部分平台不支持 CBUFFER 中的 uint 类型，改为 float 并在使用处强制转换
- **避免 SurfaceInput.hlsl 冲突**: 自定义采样函数使用 Custom 前缀，不重复声明已由 SurfaceInput.hlsl 声明的纹理
- **深度纹理使用 DeclareDepthTexture.hlsl**: 使用 URP 的 SampleSceneDepth() 并通过 ConfigureInput(Depth) 确保深度拷贝先于光照 Pass

### 代码审查修复
- CRITICAL: `renderingData.commandBuffer` 为 internal 字段 → 改用 CommandBufferPool
- CRITICAL: CBUFFER 中 uint 不兼容 SRP Batcher → 改为 float
- CRITICAL: _CameraDepthTexture 重复声明 → 使用 DeclareDepthTexture.hlsl
- IMPORTANT: ShadowCaster/DepthOnly Pass 缺少材质输入 → 添加 CustomDeferredLitInput.hlsl include
- IMPORTANT: SampleNormal/SampleEmission 与 SurfaceInput.hlsl 冲突 → 重命名为 CustomSample 前缀
- IMPORTANT: 环境光仅使用 SH DC 项 → 改用 SampleSH(normalWS) 完整 L2 球谐

### 测试
- 编辑器测试因 Tuanjie 编辑器已打开项目无法通过 CLI 运行（需在编辑器内 Test Runner 运行）
- 测试用例总数: 37 (Editor: 31, Runtime: 6)
- 测试覆盖: GBuffer 编码解码、着色模型 ID、配置验证、管线集成

### 备注
- Phase 1 仅实现 DefaultLit 着色模型，后续 Phase 2 添加 Subsurface/ClearCoat/TwoSidedFoliage
- Mobile 路径 (3 RT) 的着色器变体需在 Phase 3 中通过 keyword 切换实现
- 需在编辑器内将 CustomDeferredRendererFeature 添加到 PC_High_Renderer 并创建 CustomDeferredConfig 资产
- Stencil 光源裁剪留到 Phase 4
---

## [2026-03-30] 项目工程化基础设施搭建

### 范围
- 建立项目迭代管理机制（迭代日志、待办清单）
- 创建 `tuanjie-game-dev` 开发工作流 Skill

### 变更
- `迭代日志.md` - 新建，用于记录每次迭代详情
- `待办清单.md` - 新建，用于管理项目待办任务

### 设计决策
- 迭代日志采用倒序排列，便于查看最新变更
- 待办清单分为 Pending / In Progress / Completed 三个状态区域
- 所有 Debug.Log 要求使用 `ENABLE_DEBUG_LOG` 宏包裹
- 单元测试要求使用 `ENABLE_UNIT_TESTS` 宏开关控制

### 测试
- 无（基础设施文件，不涉及代码测试）

### 备注
- 后续所有代码迭代必须严格遵循此日志格式记录
---
